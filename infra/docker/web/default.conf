server {
  listen 80;
  server_name _;

  location = /healthz {
    default_type application/json;
    return 200 '{"status":"ok","service":"web"}';
  }

  location / {
    default_type text/plain;
    return 200 'worldcup-ai-prediction web dev container is running\n';
  }
}

